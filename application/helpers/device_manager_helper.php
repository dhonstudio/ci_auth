<?php
    $ci = get_instance();

    /*
    | -------------------------------------------------------------------
    |  Set up this if there is device manager
    | -------------------------------------------------------------------
    */
    $ci->table_devices    = 'devices';
    $ci->table_addresses  = 'addresses';
    $ci->table_u_devices  = 'user_device';